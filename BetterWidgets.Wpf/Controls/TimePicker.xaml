<UserControl
    x:Class="BetterWidgets.Controls.TimePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BetterWidgets.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="Root"
    d:DesignHeight="38"
    d:DesignWidth="200"
    mc:Ignorable="d">

    <Grid x:Name="RootLayout">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ui:Card
            Grid.ColumnSpan="3"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch" />

        <ComboBox
            x:Name="UIHourPicker"
            Grid.Column="0"
            ItemsSource="{Binding ElementName=Root, Path=Hours, Mode=OneWay}"
            SelectionChanged="OnHourSelectionChanged"
            Style="{StaticResource PickerComboBoxStyle}" />

        <ComboBox
            x:Name="UIMinutePicker"
            Grid.Column="1"
            ItemsSource="{Binding ElementName=Root, Path=Minutes, Mode=OneWay}"
            SelectionChanged="OnMinuteSelectionChanged"
            Style="{StaticResource PickerComboBoxStyle}" />

        <ComboBox
            x:Name="UIMeridiemPicker"
            Grid.Column="2"
            SelectedIndex="{Binding ElementName=Root, Path=TimeType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            Style="{StaticResource PickerComboBoxStyle}">
            <ComboBox.Items>
                <ComboBoxItem Content="AM" />
                <ComboBoxItem Content="PM" />
            </ComboBox.Items>
        </ComboBox>

        <Separator
            Grid.Column="0"
            Margin="0,3"
            HorizontalAlignment="Right"
            VerticalAlignment="Stretch" />

        <Separator
            Grid.Column="1"
            Margin="0,3"
            HorizontalAlignment="Right"
            VerticalAlignment="Stretch" />
    </Grid>

</UserControl>
