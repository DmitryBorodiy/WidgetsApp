<Page
    x:Class="BetterWidgets.Views.Dialogs.PickCalendarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:r="clr-namespace:BetterWidgets.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:BetterWidgets.ViewModel.Dialogs"
    x:Name="Root"
    Title="PickCalendarView"
    d:Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    d:DesignHeight="230"
    d:DesignWidth="230"
    d:Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Page.DataContext>
        <vm:PickCalendarDialogViewModel />
    </Page.DataContext>

    <Grid x:Name="RootLayout">
        <ui:ListView
            ItemTemplate="{StaticResource CalendarItemTemplate}"
            ItemsSource="{Binding Calendars, Mode=OneWay}"
            SelectedItem="{Binding SelectedCalendar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <ui:Button
            Width="30"
            Height="30"
            Margin="-5,-5,-6,-5"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Command="{Binding UpdateCommand}"
            CommandParameter="{StaticResource True}"
            Style="{StaticResource FlatButton}"
            ToolTipService.ToolTip="{x:Static r:Resources.Refresh}">
            <ui:Button.Icon>
                <ui:SymbolIcon FontSize="18" Symbol="ArrowClockwise20" />
            </ui:Button.Icon>
        </ui:Button>
    </Grid>
</Page>
