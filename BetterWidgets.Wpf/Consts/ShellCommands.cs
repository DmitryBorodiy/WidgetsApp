namespace BetterWidgets.Consts
{
    public class ShellCommands
    {
        public const string widgets = nameof(widgets);

        public const string addwidget = nameof(addwidget);
        public const string hidewidget = nameof(hidewidget);
        public const string settings = nameof(settings);
    }
}
